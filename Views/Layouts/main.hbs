<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css" rel="stylesheet" integrity="sha384-QWTKZyjpPEjISv5WaRU9OFeRpok6YctnYmDr5pNlyT2bRjXh0JMhjY6hW+ALEwIH" crossorigin="anonymous">
    <title>MitaLent</title>
    <style>
        body {
            background-image: url('/fondos/fondologin1.jpg'); /* Ruta de la imagen en la carpeta public */
        background-size: cover; /* Asegura que la imagen cubra toda la pantalla */
        background-position: center; /* Centra la imagen */
        background-repeat: no-repeat; /* Evita que la imagen se repita */
        min-height: 100vh; /* Asegura que cubra toda la pantalla */
        color: rgb(5, 5, 5); /* Cambia el color del texto si es necesario */
        }

        /* Asegúrate de que la contenedor tenga un ancho máximo adecuado */
        .container {
            max-width: 800px; /* O el ancho que desees */
        }

        /* Estilo para mejorar la apariencia en móviles */
        .logo {
            width: 100%;  /* Asegura que el logo se ajuste a la pantalla */
            max-width: 300px;  /* Limita el tamaño máximo del logo */
            height: auto;  /* Mantiene la relación de aspecto */
            border-radius: 50%;  /* Redondea el logo */
        }
        
        .content {
            padding: 20px; /* Agrega algo de espacio interior */
        }
        
        /* Ajustes de estilo para texto y botones en móviles */
        @media (max-width: 576px) {
            .content {
                padding: 10px; /* Reduce el padding en móviles */
            }
            .btn {
                width: 100%; /* Hacer botones del ancho completo en móviles */
            }
        }
    </style>
</head>
<body>

    {{> Navbar}}

    <div id="successPopup" class="alert alert-success" style="display: {{#if successMessage}}block{{else}}none{{/if}};">
        <div id="popupMessage">{{successMessage}}</div>
    </div>
 
    <div id="errorPopup" class="alert alert-danger" style="display: {{#if errorMessage}}block{{else}}none{{/if}};">
        <div id="errorMessage">{{errorMessage}}</div>
    </div>

    <div class="container d-flex justify-content-center align-items-center flex-column" style="min-height: 100vh;">
        <div class="content w-100">
            {{{body}}}
        </div>
    </div>

    {{#if successMessage.length}}
    <script>
        document.getElementById('successPopup').style.display = 'block';
        document.getElementById('popupMessage').textContent = '{{successMessage}}';
        // Ocultar el mensaje de éxito después de 3 segundos
        setTimeout(() => {
            document.getElementById('successPopup').style.display = 'none';
        }, 3000);
    </script>
    {{/if}}

    {{#if errorMessage.length}}
    <script>
        document.getElementById('errorPopup').style.display = 'block';
        document.getElementById('errorMessage').textContent = '{{errorMessage}}';
        // Ocultar el mensaje de error después de 3 segundos
        setTimeout(() => {
            document.getElementById('errorPopup').style.display = 'none';
        }, 3000);
    </script>
    {{/if}}

    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/js/bootstrap.bundle.min.js" integrity="sha384-YvpcrYf0tY3lHB60NNkmXc5s9fDVZLESaAA55NDzOxhy9GkcIdslK1eN7N6jIeHz" crossorigin="anonymous"></script>
    <link href="https://cdnjs.cloudflare.com/ajax/libs/lightbox2/2.11.3/css/lightbox.min.css" rel="stylesheet" />

    <script src="https://cdnjs.cloudflare.com/ajax/libs/jquery/3.6.0/jquery.min.js"></script>
    <script src="https://cdnjs.cloudflare.com/ajax/libs/lightbox2/2.11.3/js/lightbox-plus-jquery.min.js"></script>

    <script src="/js/app.js"></script>
   
</body>
</html>
